const r=JSON.parse(localStorage.getItem('chitranshLastResult')||'null');if(!r){document.querySelector('main').innerHTML='<section class="panel"><h2>No result found.</h2><a class="btn primary" href="index.html">Back Home</a></section>'}else{document.getElementById('resultTitle').textContent=r.testName;document.getElementById('score').textContent=Number(r.score).toFixed(2).replace('.00','');document.getElementById('correct').textContent=r.correct;document.getElementById('wrong').textContent=r.wrong;document.getElementById('skipped').textContent=r.skipped;document.getElementById('accuracy').textContent=r.accuracy.toFixed(1)+'%';document.getElementById('summary').textContent=`${r.correct} correct, ${r.wrong} wrong aur ${r.skipped} skipped. Negative marking: −${r.negative} per wrong.`;document.getElementById('details').innerHTML=`<div class="detail-line"><span>Total Questions</span><strong>${r.total}</strong></div><div class="detail-line"><span>Marks from Correct</span><strong>+${r.correct}</strong></div><div class="detail-line"><span>Negative Marks</span><strong>−${(r.wrong*parseFloat(r.negative)).toFixed(2)}</strong></div><div class="detail-line"><span>Attempted</span><strong>${r.correct+r.wrong}</strong></div>`;document.getElementById('retry').href=`test.html?id=${encodeURIComponent(r.testId)}`}
